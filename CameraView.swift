import SwiftUI
struct CameraView: View {
    var body: some View {
        VStack {
            Text("Light Sensor Coming Soon...")
                .font(.title)
                .padding()
        }
        .navigationTitle("Light Sensor")
    }
}

struct CameraView_Previews: PreviewProvider {
    static var previews: some View {
        CameraView()
    }
}

